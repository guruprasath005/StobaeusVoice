OPENAI_API_KEY=sk-...
JWT_SECRET=generate-a-long-random-string-here
# PostgreSQL (recommended — matches production):
DATABASE_URL=postgresql://USER@localhost:5432/stobaeus
# SQLite fallback (no server needed): DATABASE_URL=sqlite:///./stobaeus.db
